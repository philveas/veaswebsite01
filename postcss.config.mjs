// postcss.config.mjs

export default {
  plugins: {
    // Keep Tailwind CSS PostCSS plugin
    "@tailwindcss/postcss": {},
    // Keep Autoprefixer, which is crucial for browser compatibility
    "autoprefixer": {}, 
  },
};